# Build Encryption Secret
# CRITICAL: Keep this secret! Used to encrypt Ultra modules at build time
# Generate with: openssl rand -hex 32
BUILD_ENCRYPTION_SECRET=replace-with-64-hexadecimal-characters
